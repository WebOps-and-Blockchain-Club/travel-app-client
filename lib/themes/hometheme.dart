import 'package:flutter/material.dart';

class modestheme extends StatelessWidget {
  final String label;
  final String entry;
  const modestheme({
    Key? key,
    required this.label,
    required this.entry,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
